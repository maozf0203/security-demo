server:
  port: 8881
spring:
  application:
    name: auth-server
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
  redis:
    host: localhost
    port: 6379
    #password: root
  session:
    store-type: redis
    timeout: 1800
logging:
  level:
     com:
      tuling:
        config:
          role:
            mapper: debug

